// Public endpoint only. Generate with npm run ai:configure after completing .env.
// Never add keys, credentials, tokens, or secrets to this file.
window.ROGGY_AI_CONFIG = Object.freeze({ bridgeUrl: '' });
